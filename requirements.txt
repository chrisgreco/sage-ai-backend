# Core dependencies for LiveKit agent and backend
# python-dotenv removed - Render manages environment variables directly

# FastAPI backend dependencies - EXPLICIT VERSIONS FOR STABILITY
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic==2.5.0

# LiveKit dependencies - ENHANCED FULL VOICE AI STACK
# Using the latest 1.1.x series for bug fixes and improvements
livekit-agents[deepgram,openai,silero,turn-detector]~=1.1.5
python-dotenv

# Audio/Video processing required by LiveKit
av>=10.0.0

# AI dependencies for LLM and TTS
openai>=1.84.0

# HTTP client for API calls
httpx>=0.25.0

# Supabase for memory management
supabase>=2.0.0

# Additional utilities
asyncio-mqtt>=0.13.0
psutil>=5.9.0

# Additional dependencies
aiofiles>=23.0.0
